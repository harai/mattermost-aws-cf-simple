name: Grub
description: Generated by ${STACK}
schemaVersion: 1.0
phases:
- name: build
  steps:
  - name: EnablePSI
    action: ExecuteBash
    inputs:
      commands:
      - sed -i 's/GRUB_CMDLINE_LINUX_DEFAULT="/GRUB_CMDLINE_LINUX_DEFAULT="psi=1 /g' /etc/default/grub
      - grub2-mkconfig -o /boot/grub2/grub.cfg
